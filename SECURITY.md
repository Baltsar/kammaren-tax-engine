# Security

This engine processes no user data server-side. All calculations run client-side in the browser or locally via Node.js/Bun.

If you find a calculation error that could lead to incorrect tax advice, please report it as a GitHub issue with full reproduction steps. Tax calculation errors are treated as security-critical.
